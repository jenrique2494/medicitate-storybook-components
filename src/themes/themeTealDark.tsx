import { createTheme } from "@mui/material/styles";


export const themeTealDark = createTheme({
    palette: {
        mode: 'dark',
        primary: {
          main: '#00695c',
        },
        secondary: {
          main: '#607d8b',
        },
    },
});

export default themeTealDark;